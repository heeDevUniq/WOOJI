<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wooji.mapper.UserMapper">

    <insert id="insertUser" parameterType="map" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (
            email
        ,   password
        ,   nickname
        ) VALUES (
            #{email}
        ,   #{password}
        ,   #{nickname}
        )
    </insert>

    <select id="selectUserByEmail" parameterType="string" resultType="map">
        SELECT  user_id
        ,       email
        ,       password
        ,       nickname
        ,       profile_image
        ,       deleted_yn
        ,       created_at
        FROM    users
        WHERE   email = #{email}
        AND     deleted_yn = 'N'
    </select>

    <select id="selectUserByProvider" resultType="map">
        SELECT  user_id
        ,       email
        ,       password
        ,       nickname
        ,       profile_image
        ,       provider
        ,       provider_id
        ,       created_at
        FROM    users
        WHERE   provider = #{provider}
        AND     provider_id = #{providerId}
        AND     deleted_yn = 'N'
    </select>

    <insert id="insertSocialUser" parameterType="map" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (
            email
        ,   nickname
        ,   profile_image
        ,   provider
        ,   provider_id
        ) VALUES (
            #{email}
        ,   #{nickname}
        ,   #{profileImage}
        ,   #{provider}
        ,   #{providerId}
        )
    </insert>

    <update id="updateProvider" parameterType="map">
        UPDATE  users
        SET     provider = #{provider}
        ,       provider_id = #{providerId}
        WHERE   user_id = #{userId}
    </update>

    <select id="selectUserById" parameterType="long" resultType="map">
        SELECT  user_id, email, nickname, profile_image, created_at
        FROM    users
        WHERE   user_id = #{userId}
        AND     deleted_yn = 'N'
    </select>

    <select id="countByEmail" parameterType="string" resultType="int">
        SELECT  COUNT(*)
        FROM    users
        WHERE   email = #{email}
        AND     deleted_yn = 'N'
    </select>

    <update id="updatePassword" parameterType="map">
        UPDATE  users
        SET     password = #{password}
        WHERE   user_id = #{userId}
    </update>

    <update id="updateProfile" parameterType="map">
        UPDATE  users
        SET     nickname = #{nickname}
        WHERE   user_id = #{userId}
    </update>

    <update id="updateProfileImage" parameterType="map">
        UPDATE  users
        SET     profile_image = #{profileImage}
        WHERE   user_id = #{userId}
    </update>

    <update id="deleteUser" parameterType="long">
        UPDATE  users
        SET     deleted_yn = 'Y'
        WHERE   user_id = #{userId}
    </update>

    <insert id="insertRefreshToken" parameterType="map">
        INSERT INTO refresh_tokens (
            user_id
        ,   token
        ,   expires_at
        ) VALUES (
            #{userId}
        ,   #{token}
        ,   #{expiresAt}
        )
    </insert>

    <select id="selectRefreshToken" parameterType="string" resultType="map">
        SELECT  token_id
        ,       user_id
        ,       token
        ,       expires_at
        FROM    refresh_tokens
        WHERE   token = #{token}
    </select>

    <delete id="deleteRefreshToken" parameterType="string">
        DELETE FROM refresh_tokens
        WHERE       token = #{token}
    </delete>

    <delete id="deleteRefreshTokenByUser" parameterType="long">
        DELETE FROM refresh_tokens
        WHERE       user_id = #{userId}
    </delete>

</mapper>
